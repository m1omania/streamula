# Инструкция по запуску режима трансляции

## Запуск приложения

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск WebSocket signaling сервера
В отдельном терминале:
```bash
npm run server
```
Сервер запустится на порту 8080 (ws://localhost:8080)

### 3. Запуск приложения
В другом терминале:
```bash
npm run dev
```
Приложение будет доступно по адресу http://localhost:5173

## Использование

### Для ведущего (Broadcaster):

1. Откройте студию (http://localhost:5173)
2. Нажмите кнопку **"Начать"** в нижней панели управления
3. Приложение автоматически:
   - Сгенерирует уникальный ID трансляции
   - Подключится к WebSocket серверу
   - Начнет захват видео/аудио потока
   - Создаст WebRTC соединение

4. Скопируйте ссылку для зрителей: `/watch/{streamId}`
   - StreamId можно увидеть в консоли браузера или в store

### Для зрителей (Viewers):

1. Откройте ссылку: `http://localhost:5173/watch/{streamId}`
2. Страница автоматически:
   - Подключится к WebSocket серверу
   - Установит WebRTC соединение с ведущим
   - Начнет отображать трансляцию

3. Доступные функции:
   - Просмотр трансляции в реальном времени
   - Чат (сообщения отправляются через WebSocket)
   - Реакции (👍 ❤️ 😂 👏)

## Технические детали

- **WebSocket Signaling Server**: `server/signaling-server.js` (порт 8080)
- **WebRTC**: Peer-to-peer соединение через STUN серверы Google
- **Роутинг**: React Router для страницы просмотра `/watch/:streamId`

## Отладка

- Проверьте консоль браузера для логов WebRTC
- Проверьте логи signaling сервера в терминале
- Убедитесь, что оба сервера запущены (dev и signaling)

## Примечания

- Для локального тестирования используется `getUserMedia` (камера/микрофон)
- В будущем можно заменить на захват canvas/сцены
- WebRTC требует HTTPS для production (или localhost для разработки)

